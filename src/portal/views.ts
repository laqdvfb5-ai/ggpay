function esc(v:unknown){return String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]!));}
const css=`body{font:14px system-ui;margin:0;background:#f5f7fb;color:#172033}nav{background:#111827;color:white;padding:14px 24px;display:flex;gap:20px}main{max-width:960px;margin:24px auto;padding:0 16px}.card{background:white;border:1px solid #dde3ee;border-radius:10px;padding:18px;margin:14px 0}table{width:100%;border-collapse:collapse}th,td{text-align:left;padding:10px;border-bottom:1px solid #eee}button,.btn{display:inline-block;background:#2563eb;color:white;border:0;border-radius:6px;padding:9px 14px;text-decoration:none;cursor:pointer}.danger{background:#b91c1c}.muted{color:#64748b}.ok{color:#15803d}form.inline{display:inline}`;
function layout(title:string,body:string){return`<!doctype html><html lang="vi"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width"><title>${esc(title)} · GGPay</title><style>${css}</style></head><body><nav><b>GGPay Portal</b></nav><main>${body}</main></body></html>`;}
export function portalLogin(message=''){return layout('Đăng nhập',`<div class="card"><h1>Đăng nhập khách hàng</h1><p>${esc(message||'Dùng liên kết đăng nhập được GGPay gửi tới email của bạn.')}</p></div>`);}
export function portalPage(data:any,csrf:string,message=''){const c=data.connection,rows=data.accounts.map((a:any)=>`<tr><td>${esc(a.bank_code)}</td><td>${esc(a.account_number)}</td><td>${esc(a.label)}</td><td>${esc(a.sync_status)}</td><td>${a.source==='sepay_oauth'?`<form class="inline" method="post" action="/portal/bank-accounts/${a.id}/${a.active?'disable':'enable'}"><input type="hidden" name="_csrf" value="${esc(csrf)}"><button class="${a.active?'danger':''}">${a.active?'Tắt':'Bật nhận giao dịch'}</button></form>`:'Tài khoản cũ — cần kết nối lại'}</td></tr>`).join('');return layout(data.tenant.name,`<h1>${esc(data.tenant.name)}</h1>${message?`<div class="card">${esc(message)}</div>`:''}<div class="card"><h2>Kết nối SePay</h2><p>Trạng thái: <b>${esc(c?.status??'Chưa kết nối')}</b></p>${c?.last_error?`<p class="danger">${esc(c.last_error)}</p>`:''}<form class="inline" method="post" action="/portal/sepay/connect"><input type="hidden" name="_csrf" value="${esc(csrf)}"><button>${c?.status==='active'?'Kết nối lại SePay':'Kết nối SePay'}</button></form> <a class="btn" target="_blank" rel="noopener" href="https://my.sepay.vn/bankaccount/connect">Thêm tài khoản trên SePay</a>${c?.status==='active'?`<form class="inline" method="post" action="/portal/sepay/sync"><input type="hidden" name="_csrf" value="${esc(csrf)}"><button>Đồng bộ tài khoản</button></form>`:''}<p class="muted">Bạn tự đăng nhập và kết nối ngân hàng trên SePay. GGPay không nhận mật khẩu ngân hàng, mật khẩu SePay hoặc OTP.</p></div><div class="card"><h2>Tài khoản ngân hàng</h2><table><tr><th>Ngân hàng</th><th>Số tài khoản</th><th>Tên</th><th>Trạng thái</th><th></th></tr>${rows}</table></div><form method="post" action="/portal/logout"><input type="hidden" name="_csrf" value="${esc(csrf)}"><button class="danger">Đăng xuất</button></form>`);}
